# Review Bundle

## Summary

- Added the first minimal `important_attributes` starter slice to compact `entities.items`.
- Kept the existing compact entity fields unchanged: `entity_id`, derived `domain`, direct `state`, and direct plain-string `friendly_name`.
- Derived `important_attributes` only from the already readable `/states.attributes` payload.
- Used a deliberately tiny generic whitelist: `device_class`, `state_class`, and `unit_of_measurement`.
- Included only string attribute values and omitted `important_attributes` when none of the whitelisted keys were present as strings.
- Did not duplicate `friendly_name` inside `important_attributes`.
- Did not introduce `raw_attributes`, broad/domain-specific attribute handling, relationship modeling, dashboard logic, areas/devices logic, integrations logic, YAML/file parsing, WebSocket access, POST/template fallback, service calls, or state-changing behavior.
- Preserved existing `/states` unreadable behavior: `entities.items` is still omitted when `/states` is not readable.
- Bumped the application/add-on version from `0.0.10` to `0.0.11` for the functional export shape change.

## Changed files

- `ha_ai_context_exporter/rootfs/app/export/export_entities.py`
- `ha_ai_context_exporter/rootfs/app/version.py`
- `ha_ai_context_exporter/config.yaml`
- `docs/ai/AI_CURRENT_STATE.md`
- `docs/ai/AI_CHANGE_HISTORY.md`
- `CHANGELOG.md`
- `review_bundle.md`
- `docs/review_bundles/2026-04-19_0.0.11_important-attributes-starter.md`

## Validation

- Current branch ref was observed as `refs/heads/important_attributes-Starter`; the branch ref matched `main` before edits.
- `git` was not available on the PowerShell PATH in this environment, so normal `git status` / `git diff` validation could not be run here.
- `python --version` reported Python 3.12.10.
- Bytecode-based `py_compile` hit an existing Windows `__pycache__` access-denied condition; no-bytecode `compile()` syntax validation was run successfully for the touched Python files.
- Focused helper validation confirmed compact entity items still include stable `entity_id`, `domain`, `state`, and `friendly_name` fields.
- Focused helper validation confirmed `important_attributes` includes only string values for `device_class`, `state_class`, and `unit_of_measurement`.
- Focused helper validation confirmed non-whitelisted attributes and non-string whitelisted values are not exported.
- Focused helper validation confirmed `friendly_name` is not duplicated inside `important_attributes`.
- Focused helper validation confirmed entities without any whitelisted string attributes do not emit empty `important_attributes` objects.
- Focused helper validation confirmed unreadable `/states` semantics still omit `entities.items`.
- Focused export-payload validation confirmed `warnings` remains a list of strings and the tool version reports `0.0.11`.
- Inspection confirmed Home Assistant access remains read-only and GET-only: no POST, PUT, DELETE, PATCH, service calls, mutations, WebSocket/RPC path, template fallback, or file/YAML parsing was introduced.
- Inspection confirmed no token-like values are added to the export payload by the new entity helper logic.
- Touched Markdown files were checked for merge-conflict markers and literal escaped newline artifacts; only historical review-bundle examples mention those strings intentionally.
