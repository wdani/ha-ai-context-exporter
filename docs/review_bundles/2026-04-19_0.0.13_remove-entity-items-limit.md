# Review Bundle

## Summary

- Removed the artificial 50-item cap from compact `entities.items`.
- Kept the existing compact entity helper and item structure unchanged.
- Returned all eligible compact entity items derived from readable `/states` data.
- Preserved stable sorting by `entity_id`.
- Preserved the existing `important_attributes` whitelist behavior for string values of `device_class`, `entity_category`, `state_class`, and `unit_of_measurement`.
- Continued to omit `important_attributes` entirely when no whitelisted string values are present.
- Did not introduce `raw_attributes`, relationship modeling, dashboard logic, areas/devices logic, integrations logic, YAML/file parsing, WebSocket access, POST/template fallback, service calls, or state-changing behavior.
- Preserved existing `/states` unreadable behavior: `entities.items` is still omitted when `/states` is not readable.
- Bumped the application/add-on version from `0.0.12` to `0.0.13` for the shipped export output size/content change.

## Changed files

- `ha_ai_context_exporter/rootfs/app/export/export_entities.py`
- `ha_ai_context_exporter/rootfs/app/version.py`
- `ha_ai_context_exporter/config.yaml`
- `docs/ai/AI_CURRENT_STATE.md`
- `docs/ai/AI_CHANGE_HISTORY.md`
- `CHANGELOG.md`
- `review_bundle.md`
- `docs/review_bundles/2026-04-19_0.0.13_remove-entity-items-limit.md`

## Validation

- Current branch ref was observed as `refs/heads/remove-entity-items-limit`.
- `git` was not available on the PowerShell PATH in this environment, so normal `git status` / `git diff` validation could not be run here.
- `rg` was blocked with an access-denied error, so PowerShell `Select-String` was used for repository searches.
- No-bytecode `compile()` syntax validation passed for the touched/import-relevant Python files.
- Focused helper validation confirmed more than 50 eligible `/states` entries are returned in `entities.items`.
- Focused helper validation confirmed entity item ordering remains stable by `entity_id`.
- Focused helper validation confirmed the compact item schema remains unchanged for base fields: `entity_id`, `domain`, `state`, and `friendly_name`.
- Focused helper validation confirmed `important_attributes` behavior remains unchanged for whitelisted string values and omission of missing or non-string values.
- Focused export-payload validation confirmed `warnings` remains a list of strings and the tool version reports `0.0.13`.
- Inspection confirmed Home Assistant access remains read-only and GET-only: no POST, PUT, DELETE, PATCH, service calls, mutations, WebSocket/RPC path, template fallback, or file/YAML parsing was introduced.
- Touched Markdown files were checked for merge-conflict markers and literal escaped newline artifacts.
