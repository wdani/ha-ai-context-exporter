# Review Bundle

## Summary

- Treated this as the follow-up task `ingress-restart-ux-and-copy-polish`.
- Inspected the current 0.0.16 export-page masking status implementation, current docs, latest review bundle, and the latest real 0.0.16 export as sensitive working context.
- Confirmed `allow_sensitive_values` remains a Home Assistant add-on configuration option and `/api/info` already exposes the current read-only masking mode state.
- Reworded the export-page masking status so the primary sentence clearly says whether privacy masking is on or off for compact entity values.
- Kept the raw option name `allow_sensitive_values` only as secondary help text and kept the page read-only.
- Added lightweight read-only polling of `/api/info` so the export page refreshes masking status after an add-on restart or option-change restart.
- Added a temporary restart/reconnecting status message when `/api/info` is unavailable, with automatic retry once the backend becomes reachable again.
- Did not add a writable settings UI, config mutation endpoint, Supervisor control logic, POST/PUT/PATCH/DELETE behavior, service calls, WebSocket access, or export payload changes.
- Confirmed the latest real export did not justify masking/export behavior changes in this UX-only step.
- Bumped both `ha_ai_context_exporter/rootfs/app/version.py` and `ha_ai_context_exporter/config.yaml` from `0.0.16` to `0.0.17`.

## Changed files

- `ha_ai_context_exporter/rootfs/app/web/index.html`
- `ha_ai_context_exporter/rootfs/app/version.py`
- `ha_ai_context_exporter/config.yaml`
- `docs/ai/AI_CURRENT_STATE.md`
- `docs/ai/AI_CHANGE_HISTORY.md`
- `CHANGELOG.md`
- `review_bundle.md`
- `docs/review_bundles/2026-04-19_0.0.17_ingress-restart-ux-and-copy-polish.md`

## Validation

- Current branch ref was observed as `refs/heads/senitiv-follow-up-2`.
- `git` was not available on the PowerShell PATH in this environment, so normal branch creation, `git status`, and `git diff` validation could not be run here.
- Latest real export was parsed as sensitive working input without copying raw private values into repository documentation or review artifacts.
- Latest export inspection confirmed `tool.version = 0.0.16`, `entities.status = available`, `entities.count = 864`, and `entities.items` length 864.
- Latest export warnings confirmed the best-effort masking notice was present and the export was not generated with an `allow_sensitive_values=true` warning.
- Version validation confirmed `ha_ai_context_exporter/rootfs/app/version.py` and `ha_ai_context_exporter/config.yaml` are aligned at `0.0.17`.
- No-bytecode `compile()` syntax validation passed for `ha_ai_context_exporter/rootfs/app/version.py`.
- Static UI validation confirmed the new user-facing masking text, restart/reconnecting message, and polling constants are present.
- Static UI validation found no explicit fetch mutation methods such as POST, PUT, PATCH, or DELETE.
- Touched files were checked for merge-conflict markers.

## Remaining limitations

- This UX change cannot prevent, suppress, or replace Home Assistant Supervisor-level restart conflict messages such as another restart job already running.
- The export page only displays and refreshes the current add-on option state; users still change `allow_sensitive_values` in the Home Assistant add-on configuration.
- Masking remains best-effort only and users must manually review exports before sharing them.
- No browser runtime or Home Assistant ingress restart test was run in this local environment.
