# Review Bundle

## Summary

- Refreshed outdated AI documentation notes around `important_attributes` validation after inspecting the real 0.0.13 export.
- Confirmed the real compact entity list now contains `unit_of_measurement` and `state_class` examples in `entities.items`.
- Kept validation notes honestly open for real-export `entity_category` evidence and non-string whitelisted raw values, because the attached export did not confirm those cases.
- Performed the first targeted sensitive-values check limited to compact string-bearing fields: `entity_id`, `state`, `friendly_name`, and string values inside `important_attributes`.
- Found literal IPv4 address values in already exported compact `state` values and one plain-string `friendly_name`.
- Added a narrow compact-field correction that masks literal IPv4 address values in `state` and `friendly_name` as `[redacted_ipv4]`.
- Did not change `entity_id`, derived `domain`, sorting by `entity_id`, item structure, export key names, category names, or the `important_attributes` whitelist.
- Did not introduce `raw_attributes`, relationship modeling, dashboard logic, areas/devices logic, integrations logic, YAML/file parsing, WebSocket access, POST/template fallback, service calls, state-changing behavior, or a broad anonymization redesign.
- Bumped the application version source from `0.0.13` to `0.0.14` because exported compact field values can now be masked.

## Changed files

- `ha_ai_context_exporter/rootfs/app/export/export_entities.py`
- `ha_ai_context_exporter/rootfs/app/version.py`
- `docs/ai/AI_CURRENT_STATE.md`
- `docs/ai/AI_CHANGE_HISTORY.md`
- `CHANGELOG.md`
- `review_bundle.md`
- `docs/review_bundles/2026-04-19_0.0.14_sensitive-values-check-and-validation-notes-refresh.md`

## Validation

- Current branch ref was observed as `refs/heads/sensitive-values-check-and-validation-notes-refresh`.
- `git` was not available on the PowerShell PATH in this environment, so normal `git status` / `git diff` validation could not be run here.
- `rg` was blocked with an access-denied error, so PowerShell `Select-String` was used for repository searches.
- Attached real 0.0.13 export was parsed as sensitive working input without copying raw private values into repository documentation or review artifacts.
- Real export inspection confirmed `entities.status = available`, `entities.count = 864`, and `entities.items` length 864.
- Real export inspection confirmed compact `important_attributes` string examples for `device_class`, `state_class`, and `unit_of_measurement`.
- Real export inspection did not find compact `entity_category` examples, and could not confirm non-string whitelisted raw attribute omission because raw attributes are intentionally not exported.
- Targeted compact-field sensitive-values inspection found literal IPv4 address values in compact `state` and one plain-string `friendly_name`; no literal IPv4 address values were found in compact `entity_id` or string `important_attributes` values.
- Focused helper validation confirmed IPv4 literals are masked in compact `state` and `friendly_name` values while non-IP text is preserved.
- Focused helper validation confirmed `entity_id`, derived `domain`, stable sorting by `entity_id`, and `important_attributes` extraction behavior remain unchanged.
- No-bytecode `compile()` syntax validation passed for touched Python files.
- Inspection confirmed Home Assistant access remains read-only and GET-only: no POST, PUT, DELETE, PATCH, service calls, mutations, WebSocket/RPC path, template fallback, or file/YAML parsing was introduced.
- Touched Markdown files were checked for merge-conflict markers and literal escaped newline artifacts.
