# Review Bundle

## Summary

- Added the second small `important_attributes` refinement to compact `entities.items`.
- Extended the existing tiny generic whitelist with exactly one key: `entity_category`.
- Kept the existing compact entity fields unchanged: `entity_id`, derived `domain`, direct `state`, and direct plain-string `friendly_name`.
- Kept the existing `important_attributes` structure unchanged.
- Derived `entity_category` only from already readable `/states.attributes` data.
- Included `entity_category` only when the source value is a string.
- Preserved existing whitelist behavior for `device_class`, `state_class`, and `unit_of_measurement`.
- Continued to omit `important_attributes` entirely when no whitelisted string values are present.
- Did not duplicate `friendly_name` inside `important_attributes`.
- Did not introduce `raw_attributes`, broad/domain-specific attribute handling, relationship modeling, dashboard logic, areas/devices logic, integrations logic, YAML/file parsing, WebSocket access, POST/template fallback, service calls, or state-changing behavior.
- Preserved existing `/states` unreadable behavior: `entities.items` is still omitted when `/states` is not readable.
- Bumped the application/add-on version from `0.0.11` to `0.0.12` for the functional export shape change.

## Changed files

- `ha_ai_context_exporter/rootfs/app/export/export_entities.py`
- `ha_ai_context_exporter/rootfs/app/version.py`
- `ha_ai_context_exporter/config.yaml`
- `docs/ai/AI_CURRENT_STATE.md`
- `docs/ai/AI_CHANGE_HISTORY.md`
- `CHANGELOG.md`
- `review_bundle.md`
- `docs/review_bundles/2026-04-19_0.0.12_entity-category-important-attribute.md`

## Validation

- Current branch ref was observed as `refs/heads/entity_category`; the branch ref matched `main` before edits.
- `git` was not available on the PowerShell PATH in this environment, so normal `git status` / `git diff` validation could not be run here.
- `python --version` reported Python 3.12.10.
- No-bytecode `compile()` syntax validation passed for the touched/import-relevant Python files.
- Focused helper validation confirmed compact entity items still include stable `entity_id`, `domain`, `state`, and `friendly_name` fields.
- Focused helper validation confirmed `important_attributes` includes only string values for `device_class`, `entity_category`, `state_class`, and `unit_of_measurement`.
- Focused helper validation confirmed string `entity_category` is emitted when present.
- Focused helper validation confirmed missing and non-string `entity_category` values are omitted.
- Focused helper validation confirmed previously supported whitelist keys still behave unchanged in the same item.
- Focused helper validation confirmed non-whitelisted attributes and non-string whitelisted values are not exported.
- Focused helper validation confirmed `friendly_name` is not duplicated inside `important_attributes`.
- Focused helper validation confirmed entities without readable dict `attributes` do not emit empty `important_attributes` objects.
- Focused export-payload validation confirmed `warnings` remains a list of strings and the tool version reports `0.0.12`.
- Focused export-payload validation confirmed the expected exporter categories are still present when all default categories are requested.
- Inspection confirmed Home Assistant access remains read-only and GET-only: no POST, PUT, DELETE, PATCH, service calls, mutations, WebSocket/RPC path, template fallback, or file/YAML parsing was introduced.
- Inspection confirmed no token-like values are added to the export payload by the new entity helper logic.
- Existing broader real-world validation caveat remains: previous local observation confirmed the starter presence/omission pattern and visible `device_class` examples, but real-world examples for `unit_of_measurement`, `state_class`, and non-string whitelisted values being omitted remain pending for later user-system or community example export/test-case validation.
- Touched Markdown files were checked for merge-conflict markers and literal escaped newline artifacts; only historical review-bundle examples mention merge-conflict marker strings intentionally.
