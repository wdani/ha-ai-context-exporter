# Review Bundle

## Summary

- Treated this as the small export-core follow-up task `minimal-entity-context-follow-up` on branch `minimal-entity-context-follow-up`.
- Started from merged baseline `7b8b887` (`Merge pull request #34 from wdani/ingress-ui-container-architecture-clarification`) with a clean worktree.
- Inspected the required project memory docs, current changelog, current review bundle, add-on metadata, version source, and compact entity export helper flow before editing.
- Confirmed the current baseline is version `0.0.20`, remains ingress-based, and already has compact `entities.items` built from readable `/states` data.
- Added optional `last_changed` and `last_updated` fields inside compact `entities.items`.
- Copied those fields only when they are directly present as strings on the existing `/states` entry.
- Preserved clean omission for missing or non-string timestamp values; no placeholder text and no derived fallback timestamp is emitted.
- Reused the existing compact item construction path and existing masking pipeline instead of adding a second entity-item model.
- Extended compact masking coverage to the new string fields while keeping `allow_sensitive_values=true` as the existing opt-out.
- Preserved existing compact fields: `entity_id`, derived `domain`, direct `state`, direct plain-string `friendly_name`, and optional `important_attributes`.
- Preserved the current `important_attributes` whitelist exactly: `device_class`, `entity_category`, `state_class`, and `unit_of_measurement`.
- Preserved malformed-entry skipping and stable sorting by the original logical `entity_id` before output masking.
- Left `entities` summary semantics unchanged apart from the optional per-item timestamp fields.
- Did not add `raw_attributes`, relationship modeling, device/area/integration context, dashboard logic, YAML/file parsing, UI/container work, auth changes, transport changes, WebSocket access, POST/PUT/PATCH/DELETE behavior, service calls, or state-changing behavior.
- Bumped both `ha_ai_context_exporter/rootfs/app/version.py` and `ha_ai_context_exporter/config.yaml` from `0.0.20` to `0.0.21`.
- Updated required documentation and archived this review bundle.

## Changed files

- `ha_ai_context_exporter/rootfs/app/export/export_entities.py`
- `ha_ai_context_exporter/rootfs/app/version.py`
- `ha_ai_context_exporter/config.yaml`
- `CHANGELOG.md`
- `review_bundle.md`
- `docs/review_bundles/2026-04-19_0.0.21_direct-states-entity-timestamps.md`
- `docs/ai/AI_CURRENT_STATE.md`
- `docs/ai/AI_CHANGE_HISTORY.md`

## Validation

- Current branch: `minimal-entity-context-follow-up`.
- `git status --short --branch` confirmed the branch is active and showed the expected changed files only.
- Git executable was not on the default PowerShell PATH; validation used the bundled GitHub Desktop Git at `C:\Users\daniel\AppData\Local\GitHubDesktop\app-3.5.8\resources\app\git\cmd\git.exe`.
- Focused compact helper validation passed for direct string `last_changed` / `last_updated` inclusion.
- Focused compact helper validation passed for missing and non-string timestamp omission.
- Focused compact helper validation passed for stable original `entity_id` sorting.
- Focused compact helper validation passed for existing `entity_id`, `domain`, `state`, `friendly_name`, and `important_attributes` behavior.
- Focused compact helper validation passed for malformed-entry and non-string-state skipping.
- Focused compact helper validation passed for masking a sensitive-looking string in the new timestamp fields when `allow_sensitive_values` is false.
- Focused compact helper validation passed for preserving raw compact strings when `allow_sensitive_values=True`.
- No-bytecode `compile()` syntax validation passed for touched Python files.
- Version validation confirmed `ha_ai_context_exporter/rootfs/app/version.py` and `ha_ai_context_exporter/config.yaml` are aligned at `0.0.21`.
- Static inspection confirmed the existing Home Assistant access path remains read-only and GET-only.
- Static grep confirmed no `do_POST`, `do_PUT`, `do_PATCH`, or `do_DELETE` handlers are present in `main.py`.
- Static grep confirmed no write-capable Home Assistant request method, service call, WebSocket path, POST/template fallback, or mutation endpoint was introduced.
- Token-leak grep checked changed files; no actual token values were found. Literal source-code terms such as token header names are not secrets.
- Touched Markdown files were checked for merge-conflict markers.
- Touched Markdown files were checked for escaped newline markers.
- `git diff --check` passed.

## Remaining limitations

- No live Home Assistant export was run in this workspace, so real-environment presence of `last_changed` and `last_updated` was not independently confirmed here.
- The implementation deliberately avoids claiming timestamp availability beyond the directly readable `/states` payload; fields are copied only when present as strings.
- Masking remains best-effort only and does not claim complete anonymization.
