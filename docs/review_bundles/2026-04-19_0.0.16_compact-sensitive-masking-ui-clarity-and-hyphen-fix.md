# Review Bundle

## Summary

- Treated this as the follow-up task `compact-sensitive-masking-ui-clarity-and-hyphen-fix` on branch ref `refs/heads/senitiv-follow-up-2`.
- Inspected the current 0.0.15 compact masking implementation, add-on option surface, existing export UI, and the latest real 0.0.15 export.
- Confirmed `allow_sensitive_values` already exists as a Home Assistant add-on configuration option in `config.yaml`, but the export page did not show the current option state or where the user changes it.
- Added a small read-only export-page status line showing the current compact masking mode and pointing users to the Home Assistant add-on configuration option `allow_sensitive_values`.
- Did not add a writable in-page settings control, config mutation endpoint, or any POST/PUT/PATCH/DELETE behavior.
- Confirmed the latest export was generated with `allow_sensitive_values=true`, so raw compact values were intentionally allowed in that export.
- Used the latest export as sensitive evidence to confirm the current masker would still leave hyphenated IPv4-like fragments in compact `friendly_name` values.
- Added hyphenated IPv4-like fragment masking while preserving existing dotted and underscored IPv4 masking.
- Used the latest export as sensitive evidence to confirm a numeric measurement `state` shape that the current plain MAC-like rule would falsely redact.
- Tightened plain MAC-like masking so numeric measurement `state` values with measurement hints are preserved.
- Preserved compact item structure, original logical `entity_id` sorting, export keys, status fields, category names, and the `important_attributes` whitelist.
- Did not introduce `raw_attributes`, relationship modeling, new export sections, dashboard/areas/devices/integrations logic, YAML/file parsing, WebSocket access, POST/template fallback, service calls, state-changing behavior, or a privacy redesign.
- Bumped both `ha_ai_context_exporter/rootfs/app/version.py` and `ha_ai_context_exporter/config.yaml` from `0.0.15` to `0.0.16`.

## Changed files

- `ha_ai_context_exporter/rootfs/app/export/export_entities.py`
- `ha_ai_context_exporter/rootfs/app/main.py`
- `ha_ai_context_exporter/rootfs/app/web/index.html`
- `ha_ai_context_exporter/rootfs/app/version.py`
- `ha_ai_context_exporter/config.yaml`
- `docs/ai/AI_CURRENT_STATE.md`
- `docs/ai/AI_CHANGE_HISTORY.md`
- `CHANGELOG.md`
- `review_bundle.md`
- `docs/review_bundles/2026-04-19_0.0.16_compact-sensitive-masking-ui-clarity-and-hyphen-fix.md`

## Validation

- Current branch ref was observed as `refs/heads/senitiv-follow-up-2`.
- `git` was not available on the PowerShell PATH in this environment, so normal `git status` / `git diff` validation could not be run here.
- Attached latest real 0.0.15 export was parsed as sensitive working input without copying raw private values into repository documentation or review artifacts.
- Latest export inspection confirmed `tool.version = 0.0.15`, `entities.status = available`, `entities.count = 864`, and `entities.items` length 864.
- Latest export warnings confirmed `allow_sensitive_values=true`, so visible raw compact values in that export are expected from the configured opt-out mode.
- Focused latest-export inspection found 8 hyphenated IPv4-like fragments in compact `friendly_name` values.
- Focused simulation of the current masker confirmed those hyphenated fragments would remain before this fix and are masked after this fix.
- Focused latest-export inspection found a numeric measurement `state` shape with measurement hints that the current plain MAC-like rule would falsely redact.
- Focused simulation confirmed no compact `state` values are MAC-redacted by the new rule in that numeric measurement case.
- Focused UI/status validation confirmed `/api/info` exposes `allow_sensitive_values`, `compact_sensitive_masking`, and the add-on option source for read-only display.
- Version validation confirmed `ha_ai_context_exporter/rootfs/app/version.py` and `ha_ai_context_exporter/config.yaml` are aligned at `0.0.16`.
- No-bytecode `compile()` syntax validation passed for touched Python files.
- Inspection confirmed Home Assistant access remains read-only and GET-only: no POST, PUT, DELETE, PATCH, service calls, mutations, WebSocket/RPC path, template fallback, or file/YAML parsing was introduced.
- Touched files were checked for merge-conflict markers and literal escaped newline artifacts.

## Remaining limitations

- Masking remains best-effort only and does not claim complete anonymization.
- The export page only displays the current add-on option state; users change `allow_sensitive_values` in the Home Assistant add-on configuration.
- The scope remains limited to already exported compact string-bearing entity fields: `entity_id`, `state`, `friendly_name`, and string values inside `important_attributes`.
