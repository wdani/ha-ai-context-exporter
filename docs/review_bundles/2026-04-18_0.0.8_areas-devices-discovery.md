# Review Bundle

## Summary

- Improved Areas / Devices discovery robustness without architecture changes.
- Added a small local parser for direct list payloads and explicit wrappers from readable `/areas` and `/devices` responses.
- Preserved compact `areas_devices` semantics: `available`, `partial`, `unavailable`, and token-safe reasons.
- Kept the add-on strictly read-only with local GET requests only.
- Bumped version to `0.0.8`.

## Changed files

- `ha_ai_context_exporter/rootfs/app/main.py`
- `ha_ai_context_exporter/rootfs/app/version.py`
- `ha_ai_context_exporter/config.yaml`
- `docs/ai/AI_CURRENT_STATE.md`
- `docs/ai/AI_CHANGE_HISTORY.md`
- `CHANGELOG.md`
- `review_bundle.md`
- `docs/review_bundles/2026-04-18_0.0.8_areas-devices-discovery.md`

## Validation

- Python syntax validation was run for touched Python files.
- `/api/export` payload assembly was validated with local provider stubs.
- Areas/devices semantics were checked for both readable, only areas readable, only devices readable, reachable but unreadable, and unauthorized cases.
- `warnings` remains a list of strings.
- Inspection confirms discovery still uses local GET requests only and introduces no Home Assistant mutations.
- No token-like values were added to logs, warnings, reasons, export payload fields, or docs.
- Touched Markdown files were checked for escaped newline sequences and merge-conflict markers.
