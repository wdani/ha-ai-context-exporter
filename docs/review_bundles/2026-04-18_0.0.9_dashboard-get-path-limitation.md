# Review Bundle

## Summary

- Investigated whether dashboard discovery can gain one small additional GET-compatible reachability/readability path after the completed `0.0.9` wrapper-tolerance step.
- Kept the already completed `0.0.9` work separate: no wrapper expansion, no dashboard deep export, and no branch-on-branch reimplementation.
- Checked Home Assistant REST/WebSocket documentation and current Lovelace/frontend source shape: richer dashboard/panel access is exposed through WebSocket commands or frontend HTML, not a validated dashboard-specific REST GET path suitable for this backend model.
- No additional dashboard probe was implemented; the code remains conservative and the version stays `0.0.9`.
- Removed obsolete `docs/startvorlage für neuen chat-md` because it is not part of the active official project guidance set.

## Changed files

- `docs/ai/AI_CURRENT_STATE.md`
- `docs/ai/AI_CHANGE_HISTORY.md`
- `CHANGELOG.md`
- `review_bundle.md`
- `docs/review_bundles/2026-04-18_0.0.9_dashboard-get-path-limitation.md`
- Deleted `docs/startvorlage für neuen chat-md`

## Validation

- Python syntax validation was run for the dashboard-related Python files even though no Python code changed.
- Dashboard preview behavior was validated with focused local stubs for the investigated unreachable-current-endpoints scenario.
- Existing `0.0.9` wrapper-tolerance behavior was rechecked for wrapped dashboard lists and wrapped Lovelace config payloads.
- `warnings` remains a list of strings.
- Inspection confirms no new Home Assistant request path was added; existing dashboard discovery still uses local GET requests only and introduces no Home Assistant mutations.
- The application version remains `0.0.9` because no functional dashboard-path improvement shipped.
- No token-like values are exposed in logs, warnings, reasons, export payload fields, or docs.
- The obsolete chat-template deletion was checked against the active official AI guidance files.
- Touched Markdown files were checked for escaped newline sequences and merge-conflict markers.

