# Review Bundle

## Summary

- Treated this as the small export-core follow-up task `resolve-masked-entity-id-collisions` on branch `resolve-masked-entity-id-collisions`.
- The workspace was already on the task branch with a clean worktree when this task began.
- Started from merged baseline `b3fea79` (`Merge pull request #35 from wdani/minimal-entity-context-follow-up`).
- Inspected the required project memory docs, current changelog, current review bundle, add-on metadata, version source, and compact entity export helper flow before editing.
- Confirmed the current baseline was version `0.0.21`, remained ingress-based, and already had compact `entities.items` built from readable `/states` data.
- Preserved the current logical compact build flow: build compact entity items from readable `/states`, sort by original logical `entity_id`, return raw sorted items when `allow_sensitive_values=true`, and otherwise apply the existing masking pipeline.
- Added a small post-masking uniqueness step for compact `entities.items` output `entity_id` values.
- The uniqueness step runs only on the masked path, after existing masking has produced the output item values.
- When masking collapses multiple original entity IDs into the same masked output `entity_id`, the first masked `entity_id` remains unchanged.
- Later colliding items in original logical `entity_id` sort order get deterministic `__masked_collision_N` suffixes, such as `device_tracker.pc_redacted_ipv4__masked_collision_2` and `device_tracker.pc_redacted_ipv4__masked_collision_3`.
- The suffixing step changes only the output `entity_id` field on later masked collisions.
- Preserved existing compact fields: `entity_id`, derived `domain`, direct `state`, direct plain-string `friendly_name`, optional `important_attributes`, optional `last_changed`, and optional `last_updated`.
- Preserved the current `important_attributes` whitelist exactly: `device_class`, `entity_category`, `state_class`, and `unit_of_measurement`.
- Preserved timestamp inclusion rules: direct string `last_changed` / `last_updated` values are included, while missing or non-string values are omitted.
- Preserved malformed-entry skipping and stable sorting by the original logical `entity_id` before output masking.
- Preserved current compact masking semantics, masking labels, and `allow_sensitive_values=true` opt-out behavior.
- Did not add `original_entity_id`, reverse-lookup fields, `raw_attributes`, relationship modeling, device/area/integration context, dashboard logic, YAML/file parsing, UI/container work, auth changes, transport changes, WebSocket access, POST/PUT/PATCH/DELETE behavior, service calls, or state-changing behavior.
- Bumped both `ha_ai_context_exporter/rootfs/app/version.py` and `ha_ai_context_exporter/config.yaml` from `0.0.21` to `0.0.22`.
- Updated required documentation and archived this review bundle.

## Changed files

- `ha_ai_context_exporter/rootfs/app/export/export_entities.py`
- `ha_ai_context_exporter/rootfs/app/version.py`
- `ha_ai_context_exporter/config.yaml`
- `CHANGELOG.md`
- `review_bundle.md`
- `docs/review_bundles/2026-04-19_0.0.22_masked-entity-id-collision-deduplication.md`
- `docs/ai/AI_CURRENT_STATE.md`
- `docs/ai/AI_CHANGE_HISTORY.md`

## Validation

- Current branch: `resolve-masked-entity-id-collisions`.
- Git executable was not on the default PowerShell PATH; validation used the bundled GitHub Desktop Git at `C:\Users\daniel\AppData\Local\GitHubDesktop\app-3.5.8\resources\app\git\cmd\git.exe`.
- `rg` was present but access-denied in this environment; code inspection used PowerShell `Get-ChildItem` and `Select-String`.
- Focused compact helper validation passed for masked IPv4-fragment `entity_id` collisions.
- Focused compact helper validation confirmed the first colliding masked `entity_id` remains unchanged.
- Focused compact helper validation confirmed the second and third colliding masked `entity_id` values receive `__masked_collision_2` and `__masked_collision_3`.
- Focused compact helper validation confirmed duplicate masked output `entity_id` values are resolved deterministically.
- Focused compact helper validation confirmed sorting still uses the original logical `entity_id` before masking.
- Focused compact helper validation confirmed `allow_sensitive_values=True` returns raw compact values and does not use masked-collision suffixing.
- Focused compact helper validation confirmed existing compact fields remain present unchanged for the checked collision shape.
- Focused compact helper validation confirmed the existing `important_attributes` whitelist behavior still works for the checked collision shape.
- No-bytecode `compile()` syntax validation passed for touched Python files.
- Version validation confirmed `ha_ai_context_exporter/rootfs/app/version.py` and `ha_ai_context_exporter/config.yaml` are aligned at `0.0.22`.
- Static inspection confirmed the existing Home Assistant access path remains read-only and GET-only.
- Static grep confirmed no `do_POST`, `do_PUT`, `do_PATCH`, or `do_DELETE` handlers are present in `main.py`.
- Static grep confirmed no write-capable Home Assistant request method, service call, WebSocket path, POST/template fallback, or mutation endpoint was introduced.
- Token-leak grep checked touched files; no actual token values were found. Literal source-code and documentation terms such as person-token helpers and token header names are not secrets.
- Touched Markdown files were checked for merge-conflict markers.
- Touched Markdown files were checked for escaped newline markers.
- Touched files were checked for trailing whitespace.
- `git diff --check` passed.

## Remaining limitations

- No live Home Assistant export was run in this workspace, so real-environment collision examples were not independently observed here.
- Collision behavior was verified with focused local helper validation using realistic IPv4-fragment masking collisions; other real-export collision shapes should follow the same post-masking uniqueness path.
- Masking remains best-effort only and does not claim complete anonymization.
